name: 更新订阅地址 sources.txt

on:
  # 仅手动触发，不会自动运行
  workflow_dispatch:
    inputs:
      run_description:
        description: '运行说明（可选）'
        required: false
        default: '手动更新 sources.txt'
        type: string

jobs:
  update-sources:
    runs-on: ubuntu-latest

    steps:
      - name: 检出仓库代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # 拉取完整历史，方便 rebase

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 运行 update_sources.py
        run: |
          python update_sources.py

      - name: 检查 sources.txt 是否有变更
        id: git-check
        run: |
          if git diff --quiet HEAD -- urls/sources.txt; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "✅ sources.txt 没有变化，无需提交"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "🔄 sources.txt 已更新"
          fi

      - name: 配置 Git 用户
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 拉取最新远程代码并 rebase（解决 non-fast-forward）
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git pull --rebase origin main
          echo "✅ 已 rebase 远程最新代码"

      - name: 提交并推送更新
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git add urls/sources.txt
          git commit -m "chore: 更新 sources.txt (手动触发)"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 完成提示
        run: |
          echo "🎉 Workflow 执行完毕！"
          if [ "${{ steps.git-check.outputs.changed }}" = "true" ]; then
            echo "   sources.txt 已更新并成功推送到 main 分支"
          else
            echo "   sources.txt 无变化，本次无需提交"
          fi
